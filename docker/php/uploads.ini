; Keep PHP's transport limits in sync with CourseController's 200 MB rule.
upload_max_filesize = 200M
post_max_size = 210M

; Allow enough time for large uploads over slower connections.
max_input_time = 600
max_execution_time = 600
